# DocMind frontend (Vite) — public config. Copy to `frontend/.env`.
# These are exposed to the browser; use the Supabase *publishable/anon* key, never the secret key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=http://localhost:8000
